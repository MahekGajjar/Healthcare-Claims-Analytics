# SQL Folder

This folder will contain:

- DDL scripts to create star schema tables (`fact_claims`, `dim_member`, `dim_provider`, `dim_date`)
- Data load scripts for synthetic data
- Analytical queries, including:
  - PMPM cost calculations
  - High-cost member identification
  - Cost by diagnosis, provider specialty, and plan type
  - Trend and Cohort analysis
