# Data Folder

This folder will contain synthetic datasets for:

- `claims.csv` – claim-level data (fact table source)
- `members.csv` – member-level data (dimension source)
- `providers.csv` – provider-level data (dimension source)
- `dates.csv` – calendar dimension

> Note: All data will be synthetic and created for portfolio purposes only.
